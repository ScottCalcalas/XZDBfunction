% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Xiaopei_functions.r
\name{logist_multi_fun}
\alias{logist_multi_fun}
\title{Multivariable Logistic Regression (Categorical Predictor)}
\usage{
logist_multi_fun(
  var_model,
  var,
  response,
  data_fun,
  multi_var_all = multi_var_all
)
}
\arguments{
\item{var_model}{Character vector of variables included in the multivariable model.}

\item{var}{Character. The specific categorical variable to extract results for.}

\item{response}{Character. Name of binary outcome variable.}

\item{data_fun}{Data frame containing variables.}

\item{multi_var_all}{Character vector of full model candidate variables.
Used to determine whether \code{var} is included in \code{var_model}.}

\item{dig}{Number of digits to round OR and CI.}
}
\value{
A matrix of adjusted ORs and p-values.
}
\description{
Extracts adjusted odds ratios from a multivariable logistic regression
model. Performs Wald tests for 2-category predictors and likelihood
ratio tests (LRT) for predictors with more than 2 categories.
}
\details{
• Categorical levels are compared to the first level (reference).
• If \code{var} is not included in \code{var_model}, a blank row is returned.
• For >2 categories, LRT compares:
full model vs. model without the variable.
}
\examples{
\dontrun{
logist_multi_fun(
  var_model = c("Age", "Gender"),
  var       = "Gender",
  response  = "Y",
  data_fun  = mydata,
  multi_var_all = c("Age", "Gender", "BMI")
)
}

}
