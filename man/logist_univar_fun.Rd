% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Xiaopei_functions.r
\name{logist_univar_fun}
\alias{logist_univar_fun}
\title{Univariate Logistic Regression (Categorical Predictor)}
\usage{
logist_univar_fun(data_fun, response, var)
}
\arguments{
\item{data_fun}{Data frame containing variables.}

\item{response}{Character. Name of the binary outcome variable (0/1).}

\item{var}{Character. Name of the categorical predictor.}

\item{dig}{Integer. Number of digits for rounding OR and CI. Default = 2.}
}
\value{
A character matrix with rows = levels of the variable and
columns = \code{"OR (95\% CI)"} and \code{"p value"}.
}
\description{
Fits a univariate logistic regression model for a binary outcome and a
categorical predictor. Returns odds ratios (OR), 95\% confidence intervals,
Wald p-values for 2-level predictors, and likelihood ratio test (LRT)
p-values for predictors with >2 categories.
}
\details{
• Removes incomplete cases using only the variables in \code{response} and \code{var}.
• For categorical predictors with:
\itemize{
\item \strong{2 levels} → Wald p-value for the coefficient
\item \strong{>2 levels} → Likelihood ratio test (LRT) comparing model vs. intercept-only model
}
}
\examples{
\dontrun{
logist_univar_fun(mydata, response = "Y", var = "Gender")
}

}
